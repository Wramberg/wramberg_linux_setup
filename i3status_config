backlight {
    format = "☼ {level}%"
}

clock {
	format_time = "%H:%M:%S %Z"
}

volume_status {
	cache_timeout = 1
}

#order += "backlight"
order += "jw_net"
order += "jw_cpu"
order += "jw_mem"
order += "jw_disk"
order += "date"
order += "clock"
#order += "battery_level"

#order += "battery_level"
#order += "jw_swap"
#order += "volume_status"
#order += "eth"
#order += "wifi"
#order += "keyboard_layout"

